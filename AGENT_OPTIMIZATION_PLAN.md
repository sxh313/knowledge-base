# Agent 继续开发与优化方案

本文针对应用内 Agent（`/agent` 页面）制定后续开发方案。

当前项目已经具备一套基础 Agent 流程：AI 生成 JSON 操作计划、本地预览、用户确认后执行文档操作。下一阶段的重点不是继续堆叠操作类型，而是先提升可靠性、安全性、可审计性，再逐步增加自主整理知识库的能力。

## 一、当前实现概况

核心代码位置：

- Agent 状态与流程编排：`src/stores/agentStore.ts`
- 工具协议与 JSON 解析：`src/lib/agent/tools.ts`
- 操作预览与执行：`src/lib/agent/executor.ts`
- Agent 页面：`src/pages/Agent.tsx`
- AI 检索：`src/lib/ai/retrieval.ts`
- AI 路由与故障转移：`src/lib/ai/router.ts`

当前已经支持：

- 新建、编辑、追加、插入文档
- 读取、搜索文档
- 重命名、删除、移动分类
- 添加和移除标签
- 从文档生成知识卡片
- 执行前预览，执行后保存文档版本

当前测试基线：

- 单元测试：5 个测试文件、20 个用例通过
- 生产构建：通过
- Agent 核心逻辑：目前缺少专项测试

## 二、当前主要问题

| 优先级 | 问题 | 风险 |
| --- | --- | --- |
| P0 | 标题支持模糊匹配，可能命中错误文档 | AI 误改笔记 |
| P0 | 计划执行不是事务，多个操作可能部分成功 | 数据处于半完成状态 |
| P0 | 预览只展示描述，不展示真实内容 diff | 用户无法准确确认修改 |
| P0 | `read` 结果不会回传给 AI | 无法形成真正的“读取后再修改”闭环 |
| P1 | Agent 对话和待确认计划只存在内存 | 刷新页面后丢失 |
| P1 | JSON 只做基础解析，没有字段、长度、操作数量校验 | 异常计划可能进入执行器 |
| P1 | 删除、整体编辑等高风险操作只有普通确认 | 缺少二次保护 |
| P1 | `generateCards` 实际是本地切段，不是真正的 AI 卡片生成 | 功能名称与实际行为不一致 |
| P2 | 召回内容只有短摘要，缺少引用和置信度 | AI 容易定位错上下文 |
| P2 | 没有运行记录、耗时、失败原因和审计信息 | 难以排查问题 |

## 三、产品定位建议

建议把 Agent 设计成四种模式：

1. **问答模式**：只读知识库，不产生修改。
2. **规划模式**：生成操作计划，用户可以逐项编辑。
3. **执行模式**：用户确认后执行计划。
4. **复核模式**：执行后检查文档格式、标签、双链和卡片质量。

### 权限分级

- `read`、`search`、`list`：可自动执行。
- `create`、`append`、`addTags`、`generateCards`：默认需要确认。
- `edit`、`rename`、`move`：展示 diff 后确认。
- `delete`：必须二次确认，并明确显示文档标题、摘要和回收站行为。
- 所有写入操作优先使用 `journalId + contentHash`，不能只依赖标题。

## 四、推荐目标架构

```text
用户指令
  ↓
意图识别 / 风险分类
  ↓
检索知识库上下文
  ↓
Planner：生成结构化计划
  ↓
Schema 校验 + 权限校验
  ↓
Preview：计算真实 diff
  ↓
用户逐项确认
  ↓
Executor：事务执行
  ↓
Audit：记录结果 / 版本 / 引用
  ↓
Verifier：检查执行结果
```

### 架构改造原则

- 将 Agent 拆成 `planner`、`validator`、`preview`、`executor`、`verifier` 五个模块。
- 将现有整体 `edit` 扩展为基于锚点的 patch，减少整篇覆盖。
- 计划必须绑定 `planId`、`opId`、目标文档 `contentHash`。
- AI 输出优先使用结构化输出；不支持结构化输出的 provider 再使用 JSON 修复流程。
- 文档内容、附件内容和历史笔记都要明确标记为“不可信数据”，防止 Prompt Injection。
- 所有写入操作通过 Dexie transaction 执行，失败时整体回滚。

## 五、建议的数据模型

建议新增数据库版本，例如 v5：

```ts
AgentSession {
  id: string
  title: string
  status: 'active' | 'archived'
  createdAt: number
  updatedAt: number
}

AgentMessage {
  id: string
  sessionId: string
  role: 'user' | 'assistant' | 'tool'
  content: string
  planId?: string
  createdAt: number
}

AgentRun {
  id: string
  sessionId: string
  planId: string
  status: 'planned' | 'approved' | 'running' | 'success' | 'partial' | 'failed' | 'cancelled'
  risk: 'low' | 'medium' | 'high'
  operations: AgentOp[]
  results?: AgentOpResult[]
  createdAt: number
  finishedAt?: number
}

AgentAuditLog {
  id: string
  runId: string
  operation: string
  journalId?: string
  beforeHash?: string
  afterHash?: string
  result: 'success' | 'failed' | 'skipped'
  createdAt: number
}
```

## 六、工具能力路线

### 第一阶段：稳定已有工具

- `searchJournals`
- `readJournal`
- `createJournal`
- `appendJournal`
- `patchJournal`
- `renameJournal`
- `updateMetadata`
- `deleteJournal`
- `generateCards`

### 第二阶段：增强知识库整理

- `listRecentJournals`
- `listByTag`
- `listBySubject`
- `createBacklink`
- `findBrokenLinks`
- `mergeDuplicateJournals`
- `summarizeJournal`
- `reviewJournalQuality`
- `createStudyPlan`

### 第三阶段：有限自主整理

- 根据学习目标自动生成复习任务。
- 发现知识缺口并推荐新笔记结构。
- 自动维护索引、标签、双链和卡片。
- 定时执行“每周知识库整理”。

不建议一开始开放自动删除大量文档、自动联网写入或自动修改全库等高自主能力。

## 七、UI 优化重点

建议在 `src/pages/Agent.tsx` 中增加：

- 左侧会话列表，支持重命名、归档和删除。
- 操作计划卡片显示风险等级、影响文档、变更 diff、引用来源和操作数量。
- 每个操作单独勾选，而不是只能全部执行。
- 支持直接编辑 AI 生成的标题、标签、分类和正文。
- 显示“目标文档已被修改，请重新生成预览”。
- 执行过程中显示实时状态：检索、生成计划、预览、写入、校验。
- 增加“撤销本次运行”按钮，恢复本次版本快照。
- 对 `delete` 和整体 `edit` 使用单独的高风险确认弹窗。
- 输入框增加快捷指令模板：整理当前文档、提取知识卡片、查找重复笔记、补充双向链接。

## 八、分阶段实施 TODO

### Phase 0：安全和正确性

- [ ] 增加 `validateAgentPlan()`，校验操作类型、必填字段、最大操作数和最大内容长度。
- [ ] 禁止空查询执行全库搜索。
- [ ] 删除标题模糊自动命中；多个候选时要求用户选择。
- [ ] 给每个编辑操作增加 `expectedHash`。
- [ ] 目标文档被修改后阻止执行旧计划。
- [ ] 为 `applyPlan()` 增加 Dexie transaction。
- [ ] 为每次写入保存统一快照。
- [ ] 增加 `planId/opId`，防止重复点击造成重复写入。
- [ ] 为 Agent 核心逻辑增加单元测试。

### Phase 1：真正可用的 Agent 闭环

- [ ] 实现 `search/read → AI → 下一步操作` 的多轮工具循环。
- [ ] 工具返回结果后重新注入 AI，而不是直接结束。
- [ ] 设置最多 5 轮工具调用，防止无限循环。
- [ ] 将搜索结果改为结构化对象，不再只返回 Markdown 字符串。
- [ ] 增加来源引用：文档 ID、标题、章节和匹配片段。
- [ ] 将检索从单纯关键词升级为“高级搜索 + 分块检索 + 元数据召回”。
- [ ] 为用户附件增加独立上下文边界，明确其不是系统指令。

### Phase 2：计划和预览体验

- [ ] 为新增、追加和编辑生成真实 Markdown diff。
- [ ] 支持逐项批准、跳过和修改操作。
- [ ] 增加风险等级和影响范围。
- [ ] 显示执行前后的标题、标签和分类变化。
- [ ] 为卡片生成增加卡片预览，确认后再批量创建。
- [ ] 支持失败后继续执行或整批回滚。

### Phase 3：会话和审计

- [ ] 新增 `AgentSession`、`AgentMessage`、`AgentRun` 表。
- [ ] 页面刷新后恢复会话和待确认计划。
- [ ] 保存模型、provider、耗时、token 使用量和失败原因。
- [ ] 增加运行历史页。
- [ ] 支持按运行记录一键撤销。
- [ ] 将 Agent 运行记录纳入 GitHub 同步策略，并允许配置是否同步敏感内容。

### Phase 4：知识库增强能力

- [ ] AI 生成卡片改为结构化输出，而不是简单按段落切分。
- [ ] 增加重复文档检测和合并建议。
- [ ] 自动发现未链接提及并生成双链建议。
- [ ] 根据文档难度、标签和复习记录生成学习计划。
- [ ] 增加文档质量检查：空标题、重复内容、缺少摘要、孤立文档和失效链接。
- [ ] 支持批量整理，但默认只生成计划，不自动写入。

## 九、测试清单

- [ ] 非法 JSON、Markdown 围栏和额外文本。
- [ ] 缺少 `journalId`、标题为空和未知操作类型。
- [ ] 同名文档和模糊匹配冲突。
- [ ] 目标文档 hash 变化。
- [ ] 单个操作失败时是否整体回滚。
- [ ] 重复点击确认按钮。
- [ ] 删除、重命名、移动和标签操作。
- [ ] `insertAfter` 找不到标题时不能静默追加。
- [ ] 卡片生成数量、重复卡片和空内容。
- [ ] 刷新页面后恢复待确认计划。
- [ ] Prompt Injection 文档不会改变系统规则。
- [ ] 5 万字以上文档的截断和 token 限制。

## 十、验收指标

建议在本地记录以下指标，不上传用户内容：

- 计划解析成功率
- 首次执行成功率
- 误修改率
- 文档定位歧义率
- 用户取消率
- 平均响应时间
- 平均 token 消耗
- 工具循环次数
- 执行失败和回滚次数
- 用户撤销次数

核心目标是：误修改率接近 0，所有写入可预览、可追踪、可撤销。

## 十一、推荐实施顺序

1. 先做计划校验、精确定位、hash 防冲突、事务和审计。
2. 再做真实 diff、逐项确认和撤销。
3. 然后做 `read/search` 多轮闭环。
4. 再做 Agent 会话持久化。
5. 最后增加卡片、双链、重复整理和学习计划等高级能力。

这样可以先把 Agent 从“能执行”提升到“不会误执行”，再逐步提升自主性。

## 十二、建议新增的高价值 Agent 功能

下面的功能是在完成 Phase 0 到 Phase 2 之后最值得加入的能力。筛选标准是：使用频率高、能直接改善学习效果、结果容易验证，并且不会默认大范围修改知识库。

### 1. 主动回忆与错题复习

Agent 根据已有笔记和知识卡片生成小测验，先隐藏答案，再根据用户回答进行评分和讲解。

建议支持：

- 按主题、标签、最近学习内容生成题目。
- 支持选择题、简答题、概念解释题和代码题。
- 用户回答后给出得分、错误原因和原文引用。
- 自动记录“掌握、模糊、不会”三种状态。
- 将不会的题目加入复习队列，而不是直接创建大量新卡片。

实施 TODO：

- [ ] 新增 `startReviewSession`、`submitReviewAnswer`、`finishReviewSession`。
- [ ] 题目和答案必须绑定原始文档引用。
- [ ] 记录每道题的难度、结果和复习时间。
- [ ] 用间隔重复规则生成下一次复习日期。
- [ ] 复习结束后只生成少量高价值卡片，需用户确认后写入。

### 2. 学习目标驱动的学习计划

用户输入“我想在两周内掌握 X”后，Agent 结合现有笔记、掌握度和可用时间生成学习路线。

建议输出：

- 每日学习主题和预计用时。
- 需要先补充的基础概念。
- 每天的阅读、练习和回忆任务。
- 阶段性检查点和最终自测。
- 每项任务对应的文档、卡片和引用来源。

实施 TODO：

- [ ] 新增学习目标数据模型：目标、截止日期、每日可用时间和当前水平。
- [ ] Agent 先检索知识库，再生成计划，禁止凭空声称用户已经掌握某内容。
- [ ] 支持用户拖延、跳过和调整任务。
- [ ] 根据复习结果动态调整后续计划。
- [ ] 将计划保存为可编辑任务，不直接批量改写笔记。

### 3. 知识缺口分析

Agent 不只是回答“笔记里有什么”，还要指出“为了理解这个主题，还缺什么”。

建议流程：

1. 用户指定主题或学习目标。
2. Agent 汇总已有相关文档和卡片。
3. 输出已覆盖、部分覆盖、未覆盖三类概念。
4. 对每个缺口给出具体补充建议和优先级。

实施 TODO：

- [ ] 新增 `analyzeKnowledgeGaps` 只读工具。
- [ ] 每个判断显示依据文档和置信度。
- [ ] 区分“知识库没有”与“检索没有找到”，避免过度下结论。
- [ ] 支持将缺口转换成学习任务或待办，而不是自动生成长篇内容。

### 4. 收件箱整理助手

针对刚导入的零散内容，Agent 提供整理建议：标题、摘要、标签、分类、相关文档和可能的重复项。

建议规则：

- 默认只给建议，不自动写入。
- 一次只处理用户选中的内容或有限数量的收件箱条目。
- 新标题必须展示前后对比。
- 发现疑似重复时给出“合并建议”，不自动删除原文。

实施 TODO：

- [ ] 新增收件箱状态或 `inbox` 标签。
- [ ] 新增 `suggestJournalMetadata` 和 `findRelatedJournals`。
- [ ] 增加批量建议预览、逐项接受和跳过。
- [ ] 将接受的建议转换成已有的安全执行计划。

### 5. 编辑器内的选中内容操作

用户在文档中选中一段文字后，可以直接让 Agent 处理这段内容，减少复制粘贴和上下文丢失。

建议提供：

- 解释这段内容。
- 压缩成摘要。
- 改写成知识卡片。
- 提取术语和定义。
- 查找与当前知识库的相关文档。
- 生成双链建议。

实施 TODO：

- [ ] 将选中文本、文档 ID 和章节位置作为受限上下文传入 Agent。
- [ ] 所有写入动作仍然走计划、预览和确认流程。
- [ ] 选中文本为空或超过长度限制时给出明确提示。
- [ ] 记录“基于选中内容生成”的来源范围，便于复核。

### 6. 可信问答与引用模式

增加一个明确的“只根据我的知识库回答”模式，降低 Agent 编造内容的概率。

回答应区分：

- **知识库明确记载**：显示文档和章节引用。
- **知识库部分记载**：明确指出缺少哪些信息。
- **知识库没有找到**：直接说未找到，不把模型常识伪装成笔记内容。
- **模型推断**：单独标记为推断，并说明依据。

实施 TODO：

- [ ] 为回答增加 `sourceIds`、`matchedChunks` 和 `confidence` 字段。
- [ ] UI 支持点击引用跳转到文档原文。
- [ ] 没有足够来源时，Agent 必须先提问或建议搜索范围。
- [ ] 增加“仅知识库 / 知识库 + 常识”两种明确模式。

## 十三、暂时不建议加入的功能

以下功能看起来很强，但现阶段会显著增加误操作、成本或维护复杂度，暂不纳入开发范围：

- 自动全库重命名、自动全库分类或自动全库加标签。
- 默认自动删除、合并或覆盖文档。
- 无限制的联网搜索后自动写入知识库。
- 自动发送邮件、发布内容或修改外部服务数据。
- 复杂的多 Agent 自主讨论和无限工具循环。
- 为每个小功能单独创建一个 Agent，导致入口和权限碎片化。
- 只追求更长的回答、更多的卡片或更复杂的可视化，但没有引用和复核。

这些能力并非永远不能做，而是应该等到计划校验、事务、diff、审计、撤销和引用链路稳定后，再以受限实验功能开放。

## 十四、新功能的统一验收标准

任何新 Agent 功能都应满足以下条件后才能进入正式版本：

- [ ] 明确属于问答、规划、执行或复核中的一种模式。
- [ ] 有清晰的输入边界、输出结构和最大处理规模。
- [ ] 能显示原始文档引用或明确说明没有引用。
- [ ] 涉及写入时必须经过预览和用户确认。
- [ ] 可以取消、重试，并在必要时撤销。
- [ ] 失败不会破坏已有文档或产生重复数据。
- [ ] 有正常路径、歧义路径、超长内容和 Prompt Injection 测试。
- [ ] 能记录使用次数、成功率和用户取消率，用数据决定是否继续投入。

## 十五、推荐新增顺序

1. 先做主动回忆与复习队列，它对学习效果最直接，也最容易验证。
2. 再做可信问答与引用模式，提升日常查询的准确性。
3. 然后做编辑器选中内容操作，降低使用摩擦。
4. 接着做收件箱整理助手，改善知识库输入质量。
5. 最后做学习计划和知识缺口分析，让 Agent 从“回答问题”升级为“陪伴学习”。

每完成一个功能，都应先观察真实使用数据，再决定是否扩展为批量或自动化能力。
